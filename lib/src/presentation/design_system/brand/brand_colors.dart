import 'package:flutter/material.dart';

/// Institutional colors. Widgets should prefer semantic theme colors.
abstract final class BrandColors {
  static const uspBlue = Color(0xFF1094AB);
  static const uspLightBlue = Color(0xFF64C4D2);
  static const uspYellow = Color(0xFFFCB421);
}
